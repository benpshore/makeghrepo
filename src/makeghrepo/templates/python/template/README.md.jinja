# {{ project_name }}

{{ description }}

## Development

```sh
uv sync                  # install
uv run {{ project_name }}      # run
uv run pytest            # test
uv run ruff check        # lint
uv run ruff format       # format
```

`main` is protected: open a PR; the `ci` check must pass before merging (squash only).
